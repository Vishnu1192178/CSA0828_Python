def factorial(num):
   result = 1 if num >= 0 else None  
   for i in range(1, num + 1):
    result *= i
   return result

number = int(input("Enter a non-negative integer: "))
print(f"Factorial of {number} is: {factorial(number)}")
