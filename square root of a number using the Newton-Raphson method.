def square_root(number,epsilon=0.0001):
  guess=number/2.0
  while abs(guess**2=number)>=epsilon:
    guess=guess-((guess**2-number)/(2*guess))
    return guess
  nuumber=float(input("enter a number: "))
  sqrt=square_root(number)
  print("square root of",number,"is apprpximatliy",sqrt)
